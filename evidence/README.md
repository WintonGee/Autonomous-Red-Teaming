# Evidence

Raw evidence captured during engagements is written here, one folder per
engagement id. **Do not commit the contents of this directory** — evidence can
contain confidential data retrieved from targets (the whole point of the
"evidence over claims" principle). The directory is kept in git via `.gitkeep`;
everything else here is gitignored.
